apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bbm.fullname" . }}-test-connection"
  labels:
    {{- include "bbm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bbm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
