apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "bbm.fullname" . }}
  namespace: bb-system
  labels:
    {{- include "bbm.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "bbm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "bbm.selectorLabels" . | nindent 8 }}
    spec:
      nodeSelector:
        node-role.kubernetes.io/master: ""
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 9000
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - mountPath: /opt/devops/upload
              name: v-pvc-bb-upload
            - mountPath: /opt/devops/monitor/rules
              name: v-pvc-bb-monitor
              subPath: rules
            - mountPath: /opt/devops/monitor/servers
              name: v-pvc-bb-monitor
              subPath: servers
            - mountPath: /opt/devops/bbm.yaml
              name: v-bbm-cm
              subPath: bbm-yaml
      restartPolicy: Always
      volumes:
        - name: v-pvc-bb-upload
          persistentVolumeClaim:
            claimName: pvc-bb-upload
        - name: v-pvc-bb-monitor
          persistentVolumeClaim:
            claimName: pvc-bb-monitor
        - name: v-bbm-cm
          configMap:
            name: bbm-cm
