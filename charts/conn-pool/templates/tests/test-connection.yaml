apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "conn-pool.fullname" . }}-test-connection"
  labels:
    {{- include "conn-pool.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "conn-pool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
