apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "woqu-adapter.fullname" . }}-test-connection"
  labels:
    {{- include "woqu-adapter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "woqu-adapter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
