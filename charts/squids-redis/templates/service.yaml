apiVersion: v1
kind: Service
metadata:
  name: squids-redis
  namespace: {{.Release.Namespace}}
  labels:
    app: squids-redis
spec:
  ports:
    - port: {{ .Values.service.port }}
  selector:
    app: squids-redis